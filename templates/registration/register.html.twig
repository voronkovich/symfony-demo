{% extends 'base.html.twig' %}

{% block main %}
    <h1>{{ 'title.registration'|trans }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}

        <button type="submit" class="btn btn-primary">
            <i class="fa fa-user-plus" aria-hidden="true"></i> {{ 'action.sign_up'|trans }}
        </button>
    {{ form_end(form) }}
{% endblock %}

{% block sidebar %}
    {{ parent() }}

    {{ show_source_code(_self) }}
{% endblock %}
